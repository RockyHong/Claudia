{
  "name": "claudia",
  "version": "0.1.0",
  "description": "A personal receptionist for all your Claude Code agents",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/server",
    "packages/web"
  ],
  "bin": {
    "claudia": "./bin/cli.js",
    "claudia-standalone": "./bin/standalone.js",
    "claudia-wallpaper": "./bin/wallpaper.js"
  },
  "scripts": {
    "dev": "npm run dev --workspace=packages/server",
    "build": "npm run build --workspace=packages/web",
    "test": "vitest run",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "bundle:server": "node scripts/bundle-server.js",
    "build:sea": "node scripts/build-sea.js",
    "build:sea:x86": "node scripts/build-sea.js --arch x86",
    "build:sea:x64": "node scripts/build-sea.js --arch x64",
    "build:tauri": "npm run build:sea:x64 && npx tauri build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/RockyHong/Claudia.git"
  },
  "keywords": [
    "claude",
    "claude-code",
    "receptionist",
    "session-monitor",
    "system-tray"
  ],
  "devDependencies": {
    "esbuild": "^0.27.4",
    "postject": "^1.0.0-alpha.6",
    "vitest": "^4.1.0"
  }
}
